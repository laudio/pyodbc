dist: xenial
language: python

addons:
  apt:
    packages:
      - docker-ce

script:
  - docker build --target=base -t laudio/pyodbc .
  - docker build --target=test -t laudio/pyodbc:test . && docker run laudio/pyodbc:test
